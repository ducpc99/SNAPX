# config/mode_seq.yaml
run_name: seq_only

graph:
  enabled: false

semantics:
  use_llm: false
  use_it_fewshot: false
  it_memory:
    enabled: false

semantics_prior:
  enabled: false

guard:
  enabled: false

hybrid:
  enable_graph_reasoner: false
  enable_semantics_prior: false
  enable_guard: false
  enable_explanation: false
